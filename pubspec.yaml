name: dependency_injector_dart
description: A sample command-line application with custom built dependency injector.
version: 1.0.0
repository: https://github.com/GyurkovicsFeri/dependency-injector-dart

environment:
  sdk: ^3.1.0

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
